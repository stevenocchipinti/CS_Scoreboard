%table.scores
  %tr
    %th Player
    %th Kills
    %th Headshots

  - @stats.each do |row|
    %tr
      %td.first= row["killer"]
      %td= row["kills"]
      %td= row["headshots"]

-# TODO: This should be replaced with an ajax call
%meta{"http-equiv" => "refresh", "content" => "2"}
